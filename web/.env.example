# Base URL of the backend API.
# - Local dev (frontend on :5173, backend on :4000): http://localhost:4000/api
# - Deployed on Netlify (frontend + functions on same domain): leave unset, the
#   app defaults to the relative path "/api" which Netlify redirects to the function.
VITE_API_URL=http://localhost:4000/api
